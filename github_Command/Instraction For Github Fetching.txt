only for 1st time : git remote add upstream https://github.com/srj101/DIUTransport.git

//before work start
go github and fetch upstream
git pull origin master
yarn

//after work
git add.
git commit -m "Comment"
git push origin master
then go to github and send a pull req;
